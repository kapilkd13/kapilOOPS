package kapil.defaultpackage;

public class secondinheritance  extends inheritedClass {
	
	
	public int getSquarredSum()
	{
 return (int) (Math.pow(getA(),2)+Math.pow(getA(),2)+Math.pow(getC(),2));
		
	}

}
